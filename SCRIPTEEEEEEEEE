if getgenv().AtlanticHubLoaded then return end
getgenv().AtlanticHubLoaded = true

-- [[ SERVICES ]]
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer

-- [[ GLOBALS ]]
_G.AutoChest = false
_G.GlobalTweenSpeed = 300
_G.Moving = false
_G.Sweeping = false

-- [[ BASIC UI (NO LIBRARIES) ]]
local ScreenGui = Instance.new("ScreenGui")
local ToggleFrame = Instance.new("Frame")
local StatusLabel = Instance.new("TextLabel")
local RuntimeLabel = Instance.new("TextLabel")
local ToggleButton = Instance.new("TextButton")

ScreenGui.Name = "AtlanticHubAdvanced"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

pcall(function()
    ScreenGui.Parent = game:GetService("CoreGui")
end)
if not ScreenGui.Parent then
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
end

ToggleFrame.Name = "MainFrame"
ToggleFrame.Parent = ScreenGui
ToggleFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
ToggleFrame.BorderSizePixel = 0
ToggleFrame.Position = UDim2.new(0.05, 0, 0.3, 0)
ToggleFrame.Size = UDim2.new(0, 240, 0, 120)
ToggleFrame.Active = true
ToggleFrame.Draggable = true

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 8)
uiCorner.Parent = ToggleFrame

local uiStroke = Instance.new("UIStroke")
uiStroke.Color = Color3.fromRGB(60, 60, 60)
uiStroke.Thickness = 1.5
uiStroke.Parent = ToggleFrame

StatusLabel.Parent = ToggleFrame
StatusLabel.Size = UDim2.new(1, 0, 0, 30)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "Status: Idle"
StatusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
StatusLabel.TextSize = 14
StatusLabel.Font = Enum.Font.SourceSansBold

RuntimeLabel.Parent = ToggleFrame
RuntimeLabel.Position = UDim2.new(0, 0, 0, 25)
RuntimeLabel.Size = UDim2.new(1, 0, 0, 20)
RuntimeLabel.BackgroundTransparency = 1
RuntimeLabel.Text = "Server: 00:00:00 | Hub: 00:00:00"
RuntimeLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
RuntimeLabel.TextSize = 12
RuntimeLabel.Font = Enum.Font.SourceSans

ToggleButton.Parent = ToggleFrame
ToggleButton.Position = UDim2.new(0.1, 0, 0.45, 0)
ToggleButton.Size = UDim2.new(0.8, 0, 0, 40)
ToggleButton.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
ToggleButton.Text = "Auto Chest: OFF"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 15
ToggleButton.Font = Enum.Font.SourceSansBold

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 6)
btnCorner.Parent = ToggleButton

ToggleButton.MouseButton1Click:Connect(function()
    _G.AutoChest = not _G.AutoChest
    if _G.AutoChest then
        ToggleButton.Text = "Auto Chest: ON"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(40, 180, 40)
    else
        ToggleButton.Text = "Auto Chest: OFF"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
        StatusLabel.Text = "Status: Disabled"
        _G.Moving = false
    end
end)

-- [[ ADVANCED RUNTIME SYSTEM ]]
local hubStartTime = os.time()

local function formatTime(seconds)
    local hours = math.floor(seconds / 3600)
    local mins = math.floor((seconds % 3600) / 60)
    local secs = math.floor(seconds % 60)
    return string.format("%02d:%02d:%02d", hours, mins, secs)
end

task.spawn(function()
    while task.wait(1) do
        local serverTime = workspace.DistributedGameTime or 0
        if serverTime == 0 then
            serverTime = os.time() - hubStartTime
        end
        local hubTime = os.time() - hubStartTime
        RuntimeLabel.Text = "Server: " .. formatTime(serverTime) .. " | Hub: " .. formatTime(hubTime)
    end
end)

-- [[ MOVEMENT & FLOAT SYSTEM ]]
local block = Instance.new("Part")
block.Size = Vector3.new(1, 1, 1)
block.Name = "AtlanticHubTweenBlock"
block.Anchored = true
block.CanCollide = false
block.CanTouch = false
block.Transparency = 1
block.CFrame = CFrame.new(0, -500, 0)
block.Parent = workspace

local lastUpdate = os.clock()
RunService.Heartbeat:Connect(function()
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local hrp = char.HumanoidRootPart
    
    local currentTime = os.clock()
    local dt = currentTime - lastUpdate
    lastUpdate = currentTime
    
    -- FLOAT MECHANIC: If AutoChest is on, cancel gravity completely so you never fall
    if _G.AutoChest then
        hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
        hrp.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
    end
    
    if _G.Moving then
        local targetPos = block.Position
        local currentPos = hrp.Position
        
        -- Calculate flat horizontal distance to chest
        local flatDist = (Vector3.new(targetPos.X, 0, targetPos.Z) - Vector3.new(currentPos.X, 0, currentPos.Z)).Magnitude
        
        local desiredY = targetPos.Y
        -- If we are far away horizontally, do not drop down into the ocean. 
        -- Maintain our current height (or go higher) until we are directly above the chest.
        if flatDist > 5 then
            desiredY = math.max(currentPos.Y, targetPos.Y)
        end
        
        local adjustedTarget = Vector3.new(targetPos.X, desiredY, targetPos.Z)
        local direction = (adjustedTarget - currentPos)
        local distance = direction.Magnitude
        
        local maxStep = _G.GlobalTweenSpeed * dt
        
        if distance > maxStep then
            local moveVector = direction.Unit * maxStep
            -- Move smoothly and look towards target
            hrp.CFrame = CFrame.new(currentPos + moveVector, adjustedTarget)
        else
            -- We are close enough, snap exactly to target
            hrp.CFrame = block.CFrame
        end
        
        -- Minimal noclip to prevent getting stuck
        hrp.CanCollide = false
    end
end)

local function moveTo(targetCFrame)
    block.CFrame = targetCFrame
    _G.Moving = true
end

-- [[ HELPER FUNCTIONS ]]
local function getChests()
    local chests = {}
    for _, v in pairs(Workspace:GetDescendants()) do
        if string.find(v.Name:lower(), "chest") and (v:IsA("Part") or v:IsA("Model") or v:IsA("MeshPart")) then
            if v:IsA("Model") or (v:IsA("BasePart") and v.Size.Magnitude > 0.5) then
                table.insert(chests, v)
            end
        end
    end
    return chests
end

local function getChestPos(chest)
    if not chest or not chest.Parent then return nil end
    if chest:IsA("Model") then
        return chest:GetPivot().Position
    elseif chest:IsA("BasePart") then
        return chest.Position
    end
    return nil
end

local function isChestHit(chest)
    if not chest or not chest.Parent then return true end
    
    local hum = chest:FindFirstChildOfClass("Humanoid") or chest:FindFirstChild("Humanoid", true)
    if hum and hum.Health <= 0 then return true end
    
    local opened = chest:FindFirstChild("Opened", true) or chest:FindFirstChild("Open", true)
    if opened then
        if opened:IsA("BoolValue") and opened.Value then return true end
        if opened:IsA("IntValue") and opened.Value == 1 then return true end
    end
    
    local lowerName = chest.Name:lower()
    if string.find(lowerName, "opened") or string.find(lowerName, "broken") then return true end
    
    return false
end

-- [[ FAST AREA SWEEP (1-Second Expanding Spiral) ]]
local function fastAreaCheck(chest)
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return false end
    local hrp = char.HumanoidRootPart
    
    local chestPos = getChestPos(chest)
    if not chestPos then return false end
    
    _G.Moving = false 
    _G.Sweeping = true
    local startTime = os.clock()
    
    for i = 1, 20 do
        if isChestHit(chest) or os.clock() - startTime > 1.0 then break end
        
        local angle = i * math.rad(85)
        local radius = 0.5 + (i * 0.25)
        local offset = Vector3.new(math.cos(angle) * radius, 0, math.sin(angle) * radius)
        
        local targetCFrame = CFrame.new(chestPos + offset, chestPos)
        hrp.CFrame = hrp.CFrame:Lerp(targetCFrame, 0.8)
        
        -- Keep velocity zeroed while sweeping so gravity doesn't pull you down
        hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
        
        task.wait(0.05)
    end
    _G.Sweeping = false
    
    return isChestHit(chest)
end

-- [[ AUTO CHEST FARM ]]
task.spawn(function()
    while task.wait(0.5) do
        if _G.AutoChest then
            local chests = getChests()
            StatusLabel.Text = "Status: Found " .. #chests .. " Chests"
            
            for _, chest in ipairs(chests) do
                if not _G.AutoChest then break end
                if chest and chest.Parent and not isChestHit(chest) then
                    local chestPos = getChestPos(chest)
                    if chestPos then
                        StatusLabel.Text = "Status: Moving to " .. chest.Name
                        
                        moveTo(CFrame.new(chestPos))
                        
                        local timeout = os.clock() + 5
                        local char = LocalPlayer.Character
                        while _G.AutoChest and char and char:FindFirstChild("HumanoidRootPart") and os.clock() < timeout do
                            local hrp = char.HumanoidRootPart
                            -- Wait until we are perfectly above or very close to the chest
                            local flatDist = (Vector3.new(chestPos.X, 0, chestPos.Z) - Vector3.new(hrp.Position.X, 0, hrp.Position.Z)).Magnitude
                            if flatDist < 2 then
                                break
                            end
                            task.wait(0.1)
                        end
                        
                        _G.Moving = false
                        
                        if not _G.AutoChest then break end
                        
                        if isChestHit(chest) then
                            StatusLabel.Text = "Status: Collected " .. chest.Name
                            task.wait(0.2)
                        else
                            StatusLabel.Text = "Status: Sweeping " .. chest.Name
                            local hit = fastAreaCheck(chest)
                            
                            if hit then
                                StatusLabel.Text = "Status: Confirmed Hit " .. chest.Name
                                task.wait(0.3)
                            else
                                StatusLabel.Text = "Status: Timed Out " .. chest.Name
                                task.wait(0.5)
                            end
                        end
                    end
                end
            end
        end
    end
end)
